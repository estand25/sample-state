module.exports = {
  getAssetExts() {
    return ['pb', 'txt']
  }
}

const tfImageRecognition = new tfImageRecognition({
  model: require(''),
  labels: require(''),
});

const results = await ifImageRecognition.recognize({
  image: require(''),
})
